[pytest]
# archive/ holds dated snapshots and the curated sharing copy (copies of
# source + tests kept for hand-off); it must not be collected as part of
# the live test suite.
addopts = --ignore=archive
